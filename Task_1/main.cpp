#include <bits/stdc++.h>#include <conio.h>#include "sudipta.h"#include "likhon.h"#include "arpita.h"
#include "almas.h"using namespace std;/// MAKE SURE YOU DO NOT MAKE THE SAME NAME FUNCTION IN 2 DIFFERENT HEADER FILE IN THE SAME PROJECTint main(){	regularExpresstionMaker(input());	bool decision ;	//TheFunctionName();	char *OPERATOR;	char *PUNCTUATION;	string *IDENTIFIER;	OPERATOR = HoldOperator();	PUNCTUATION = HoldPunctuation();	IDENTIFIER = HoldIdentifier();	decision = compareExpression(OPERATOR,IDENTIFIER, PUNCTUATION);	if(decision == 1){		cout<<"Expression Matched !"<<endl;	}else{		cout<<"Expression Failed to Matched !"<<endl;	}	int choice;	int X = 0;	do{		cout<<endl<<endl<<endl;		cout<< "Press 1 to see the Symbol Table"<<endl;		cout<< "Press 0 to Exit"<<endl;		cout<< "Enter Your Choice >> ";		cin>>choice;		if(choice == 1){			printSymbolTable(OPERATOR,IDENTIFIER, PUNCTUATION);			X = 0;		}else if(choice == 0){			exit(0);		}else{			cout<< "Please Choose a Valid Option"<<endl;			X = 0;		}	}while(X == 0);	/*	int lenX = sizeof(IDENTIFIER)/sizeof(IDENTIFIER[0]);	for(int i=0;i<lenX;i++){		cout<<IDENTIFIER[i]<< " ";	}	cout<<endl;	*/    getch();}